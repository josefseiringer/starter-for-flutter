name: ptv_proxy_server
description: Production CORS proxy for PTV Geocoding API
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.5.4

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  args: ^2.6.0
  http: ^1.6.0
  dotenv: ^4.2.0

dev_dependencies:
  lints: ^5.0.0
